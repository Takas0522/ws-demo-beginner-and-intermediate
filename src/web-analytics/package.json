{
  "name": "web-analytics-collector",
  "version": "1.0.0",
  "description": "開発状況ダッシュボードから CSV データを収集する Playwright スクリプト",
  "scripts": {
    "collect": "node collect-data.js",
    "setup": "npm install && npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
